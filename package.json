{
	"name": "obsidian-journal-transcriber",
	"version": "1.0.2",
	"description": "Transcribe handwritten notes from photos into Markdown using Claude or Gemini vision models.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"plugin",
		"ocr",
		"handwriting",
		"transcription"
	],
	"author": "Josiah",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.21.5",
		"obsidian": "latest",
		"typescript": "^5.4.5"
	}
}
